#include "monty.h"

/**
  *f_nop- nothing actually
  *@head: the head of stack
  *@counter: line_num
  *Return: will not return
 */

void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
